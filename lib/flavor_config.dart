import 'package:flutter/material.dart';

class FlavorConfig {
  final String name;
  final Color color;
  final bool showBanner;

  const FlavorConfig({
    required this.name,
    required this.color,
    required this.showBanner,
  });
}
